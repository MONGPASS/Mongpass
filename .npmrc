# @cloudflare/next-on-pages@1.13.16 declares a strict peer dep on
# next@>=14.3.0 even though it works fine on Next.js 14.2.x. Tell npm
# to use the looser legacy resolution algorithm so installs (locally
# and on Cloudflare's build server) don't fail.
legacy-peer-deps=true
